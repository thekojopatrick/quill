import React from "react";

const ChatWrapper = ({
  isSubscribed,
  fieldId,
}: {
  isSubscribed: boolean;
  fieldId: string;
}) => {
  return <div>ChatWrapper</div>;
};

export default ChatWrapper;
